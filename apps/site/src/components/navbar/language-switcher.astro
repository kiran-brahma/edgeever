---
// This project is English-only; the language switcher is kept as a no-op
// placeholder so dependent imports do not break.
const { pathname, compact = false } = Astro.props;
---

<div class:list={["hidden", compact && "hidden"]}>{pathname}</div>
